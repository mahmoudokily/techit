import { ThemeVariable } from "../helpers/types";

export type ZIndex = ThemeVariable<number>;

export const zIndex = {
  buttonLoading: 10,
  fixedBox: 1000,
  loading: 10,
  modal: 1000,
  overlay: 2000,
  popover: 1000,
  routerProgressBar: 9999,
  tooltip: 1000,
  waveEffect: 10,
};
